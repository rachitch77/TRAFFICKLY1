{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs');const f='../../lib/api-zod/src/index.ts';const c=fs.readFileSync(f,'utf8').split('\\n').filter(l=>!l.includes('api.schemas')).join('\\n');fs.writeFileSync(f,c)\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.5.2"
  }
}
